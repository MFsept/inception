services:
  mariadb:
    build: ./requirements/mariadb/

    image: mariadb
    container_name: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    networks:
    - inception
    #volumes:
    #- /home/mfernand/data/maria_data:/var/lib/mysql
    volumes:
    - maria_data:/var/lib/mysql
    restart: always


  wordpress:
    build: ./requirements/wordpress/
    depends_on:
    - mariadb
    image: wordpress
    container_name: wordpress
    environment:
      MYSQL_HOST: mariadb
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MY_DOMAIN_NAME: ${MY_DOMAIN_NAME}
    networks:
    - inception
    #volumes:
    #- /home/mfernand/data/wordpress_data:/var/www/wordpress
    volumes:
    - wordpress_data:/var/www/wordpress
    restart: always



  nginx:
    build: ./requirements/nginx/
    image: nginx
    container_name: nginx
    ports:
    - "443:443"
    - 80:80
    depends_on:
    - wordpress
    networks:
    - inception
    #volumes:
    #- /home/mfernand/data/wordpress_data:/var/www/wordpress
    volumes:
    - wordpress_data:/var/www/wordpress
    restart: always

networks:
    inception:
      driver: bridge

volumes:
  maria_data:
  wordpress_data:
